import StyledSection from "./Events.styles";
import { Event } from "components";

const Events = () => {
  return (
  <StyledSection>
      <Event />
  </StyledSection>
  );
};

export default Events;
